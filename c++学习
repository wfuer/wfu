常量指针只能改变地址不能改变内存值。   const int *p = &a;  p = &b;
指针常量不能改地址可以改值   int * const p = &a;   *p = 20;
即修饰指针又修饰常量都不能改  const int * const p = &a;  无法更改。
ctrl + r 是回退

代码区：共享的  只读的
全局区：存局部变量、全局变量，常量等。 由操作系统控制生命周期
